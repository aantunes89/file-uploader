import { MouseActiveDirective } from './mouse-active.directive';

describe('MouseActiveDirective', () => {
  it('should create an instance', () => {
    const directive = new MouseActiveDirective();
    expect(directive).toBeTruthy();
  });
});
