import { MouseMoveDirective } from 'src/app/shared/directives/mouse-move.directive';

describe('MouseActiveDirective', () => {
  it('should create an instance', () => {
    const directive = new MouseMoveDirective();
    expect(directive).toBeTruthy();
  });
});
